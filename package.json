{
  "name": "bank-kata",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": "8.x"
  },
  "dependencies": {
    "body-parser": "1.18.2",
    "express": "4.16.3",
    "ws": "5.1.0"
  },
  "devDependencies": {},
  "optionalDependencies": {
    "bufferutil": "3.0.3",
    "utf-8-validate": "4.0.0"
  },
  "cacheDirectories": [
    "node_modules",
    "react-ui/node_modules"
  ],
  "scripts": {
    "start": "node server",
    "dev": "npm start & cd react-ui/ && npm start",
    "heroku-postbuild": "cd react-ui/ && npm install && npm install --only=dev --no-shrinkwrap && npm run build"
  }
}
